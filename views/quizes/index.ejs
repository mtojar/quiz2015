<h3>Estamos jugando a Quiz</h3>
<p>Puede seleccionar las preguntas e intentar acertarlas, tiene infinitas posibilidades</p>
<div style="float:right; width:100%; paddig-left:50%; height=45px; padding:2px; background-color:#E3FBE9">
  <form action="get" action="/quizes" >
    <input type="text" name="search" value="Texto a buscar" size="50" maxlength="75" />
      <input type="submit" value="Busqueda por Pregunta" />
  </form>  
</div>
<div style="float:left; width=45%; overflow:auto; height=125px;">
	<table>
  		<% var i; for (i=0; i < quizes.length; i++) { %>        
    		<tr align="left">
      		<td><a href="/quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %></a></td>
          <% if(session.user) { %>
      		  <td>
              <a href="/quizes/<%= quizes[i].id %>/edit"><button>editar</button></a></td>
            </td>
      		  <td>
              <form method="post" action="quizes/<%= quizes[i].id %>?_method=delete">
          				<button type="submit" onClick="return confirm('Borrar: <%= quizes[i].pregunta %>');"> borrar </button>
      			  </form>
      		  </td>
          <% } %>
    		</tr>
  		<% } %>
	</table>
  <% if(session.user) { %>
	   <a href="/quizes/new"><button type="button">Crear Pregunta</button></a>
  <% } %>
</div>
<div style="float:left; margin-left:40%; width=45%; overflow:auto; height=125px;">

</div>
